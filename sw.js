self.addEventListener('install', event => {  console.log('Service Worker diinstal');});self.addEventListener('activate', event => {  console.log('Service Worker diaktifkan');});self.addEventListener('notificationclick', event => {  event.notification.close();  event.waitUntil(    clients.openWindow('https://google.com') // Ganti dengan URL situs Anda  );});
